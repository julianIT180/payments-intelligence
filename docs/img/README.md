# docs/img/ — README screenshots

Three screenshots are referenced (commented out) near the top of the root README.
Capture them from the live site, save with these exact names, then uncomment the
image lines in `README.md`.

| File | What it should show |
|---|---|
| `feed.png` | The live intelligence feed at `/` — brief cards ranked by impact, the impact colour dot, category tag, confidence bar, and the "N primary" markers. This is the "at a glance, it's a real product" shot. |
| `brief-digital-euro.png` | A single brief at `/brief/<id>` (the digital euro one if still present) — the header stats (impact / confidence / sources / primary), the body with `[S#]` citation markers, and the **Sources** section showing tier badges, `via tavily` / `via rss`, and any `headline only` markers. This is the evidence-and-provenance shot. |
| `about-limitations.png` | The `/about` page, scrolled to the **Limitations** section (and ideally the "On the scores" paragraph above it). This is the intellectual-honesty / refusal-behaviour shot. |

Guidance: use a normal desktop width, light theme, crop out the browser chrome and
address bar. PNG. Keep each under ~500 KB if practical.
